# Copy this file to .env and fill in real values.
# NEVER commit .env to version control.

# API authentication (only used when security.yaml auth.enabled = true)
API_KEY=

# OCR engine credentials (only needed for non-stub engines)
AZURE_DI_ENDPOINT=
AZURE_DI_KEY=
GOOGLE_APPLICATION_CREDENTIALS=
